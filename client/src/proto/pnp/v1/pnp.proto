syntax = "proto3";

package pnp.v1;

message Message {
  Tags tag = 1;
  oneof payload { Deltas deltas = 2; }

  enum Tags {
    HEARTBEAT = 0;
    TARGET_DELTAS = 1;
    MOVED_DELTAS = 2;
  }

  message Deltas {
    sint32 x = 1;
    sint32 y = 2;
  }
}
